Setup guide of classroomfeedback
- The ``.env.local`` file is an working example,
- As this uses llama3.2 through ollama.
- also install pnpm using npm
- then ``pnpm install`` under classroomfeedback
- then ``pnpm dev`` or ``pnpm start`` to start the server at port 3000 by default